#include "stdio.h"
int main(int argc, char const *argv[]) {
        int limit,noofrows=0,noofcols=0;
        scanf("%d",&limit);
        noofcols = noofrows = (limit*2)-1;

        // printf("%d %d ",noofcols,noofrows );



        return 0;
}
